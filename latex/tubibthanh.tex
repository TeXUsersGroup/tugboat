% $Id$
% Public domain.
% 
% Test bib entry with \Thanh. We should get the definition from
% tugboat.dtx, not tugboat.def, and it should work under pdflatex,
% lualatex, and xelatex. And it should work with tagging.
% 
% See comments in tugboat.dtx at definition of \Thanh.

\DocumentMetadata{tagging=on}
\documentclass{ltugboat}
\hfuzz\maxdimen % but we don't care about overfull boxes

\ifx\luatexversion\undefined
  \ifx\XeTeXrevision\undefined
    \ifx\pdftexversion\undefined
      \def\engineident{unknown}%
      \let\engineabbr=\undefined
    \else
      \def\engineident{pdftex \the\pdftexversion}%
      \def\engineabbr{p}%
    \fi
  \else
    \def\engineident{xetex \XeTeXrevision}%
    \def\engineabbr{x}%
  \fi
\else
  \def\engineident{luatex \the\luatexversion}%
  \def\engineabbr{l}%
\fi

\ifx\engineabbr p\else
% to see that we get the desired horizontally-offset acute accent with
% the newcm fonts, can uncomment this (or the \let below):
%  \usepackage[olddefault]{fontsetup}
% The default lm fonts have the accents vertically stacked.
\fi

\begin{document}
\let\TBecircacute\TBecircacutebuilt % must be after \begin{document}

\noindent \engineident~\cite{Thanh:2000:MTE}.

\bibliographystyle{tugboat}
\bibliography{tugboat.bib}
\end{document}

% xunicode.sty defines \textcircumacute. However, it has an explicit
% test for running under XeTeX, so it fails with lualatex. And it also
% fails with xelatex:
%   ./tubbibthanh.bbl:7: LaTeX Error: Command \textcircumacute unavailable
%   in encoding TU.
% So, not helpful.
